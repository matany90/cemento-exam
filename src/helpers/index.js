/* eslint-disable */
/**
 * @param {string} projectId - relevant project id
 * @returns {Promise.<string>} - returns promise for pdf url
 */
export const generatePDF = async (projectId) => {
  // NOTE: assuming the function is implemented!
  // meanwile, we'll return an constant demo url
  return "http://www.africau.edu/images/default/sample.pdf"
}
  
/**
 * 
 * @param {string} emailDestination - email address destenation
 * @param {Array<string>} pdfUrls - pdfs urls array
 * @returns {Promise.<*>}
 */
export const sendEmail = async (emailDestination, pdfUrls) => {
  // NOTE: assuming the function is implemented!
  // meanwile, we'll return an true flag
  return true
}